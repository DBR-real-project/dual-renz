# DualGuard 미디어 분석 파트 의존성 (Python 3.9, Windows, CPU 전용)
#
# 설치:
#   py -3.9 -m venv .venv
#   .venv\Scripts\python.exe -m pip install --upgrade pip
#   .venv\Scripts\python.exe -m pip install -r requirements.txt
#
# 주의 1: torch는 CPU 전용 휠을 쓴다. 아래 --extra-index-url이 없으면
#         pip가 CUDA 포함 휠(2GB+)을 받으려 해서 해커톤 환경에서 시간을 태운다.
# 주의 2: 시스템 PATH의 `python`은 Windows 스토어 스텁이라 동작하지 않는다.
#         반드시 .venv\Scripts\python.exe 또는 py -3.9 를 쓸 것.

--extra-index-url https://download.pytorch.org/whl/cpu

# --- 딥페이크 탐지 (HuggingFace ViT 경로, 현재 동작하는 정식 경로) ---
torch==2.8.0+cpu
torchvision==0.23.0+cpu
transformers==4.57.6
pillow==11.3.0

# --- 프레임 추출 / 얼굴 검출 (dlib 불필요) ---
# 4.x로 고정한 이유: opencv-python 5.0에는 cv2.CascadeClassifier가 없다
# (objdetect 모듈이 빠짐). 5.0을 쓰면 얼굴 크롭이 AttributeError로 죽는다.
# 얼굴 검출기는 YuNet(cv2.FaceDetectorYN, opencv 4.x 내장)이 1순위,
# Haar cascade가 폴백이다. YuNet 가중치 232KB는 별도로 받는다:
#   .venv\Scripts\python.exe scripts/download_face_detector.py
opencv-python==4.11.0.86
numpy==2.0.2

# --- FaceForensics(FF++ Xception) 경로용 ---
# external/FaceForensics/classification/network/models.py가 import한다.
# 원본 레포는 dlib도 요구하지만, 얼굴 검출을 opencv Haar cascade로 대체했으므로 불필요.
pretrainedmodels==0.7.4
# pretrainedmodels가 six를 쓰면서 의존성 선언을 안 해놨다. 없으면 가중치 로드가
# ModuleNotFoundError로 죽는다.
six==1.17.0

# --- 음성 스푸핑 탐지 (AASIST) ---
# flac/wav 읽기. 영상에서 오디오를 뽑을 때는 ffmpeg CLI가 별도로 필요하다.
soundfile==0.13.1
# 리샘플링(resample_poly). 실제 통화는 전화망 8kHz거나 44.1kHz 녹음이라 거의 항상
# 리샘플링을 거치는데, 선형 보간은 저역통과 필터가 없어 에일리어싱이 생기고
# AASIST가 그걸 합성 흔적으로 오인할 수 있다. 없으면 선형 보간으로 폴백한다.
scipy==1.13.1

# --- 점수 재척도 파라미터 적합 (개발용, 런타임에는 불필요) ---
# scripts/calibrate_deepfake.py 에서만 쓴다. 적합 결과는
# data_seed/deepfake_calibration.json 에 저장돼 저장소에 들어가므로,
# 서비스만 돌릴 거라면 이 패키지는 없어도 된다.
scikit-learn==1.6.1

# --- 데이터셋 부분 추출 ---
# ASVspoof 샘플이 parquet으로 배포돼 있어 row group 단위로 읽는 데 쓴다.
pyarrow==21.0.0
# Kaggle 데이터(DFDC 등) 다운로드용. 자격증명이 없으면 스크립트가 안내만 하고 종료한다.
kaggle==1.7.4.5

# --- 콘텐츠 분석: STT ---
# openai-whisper가 아니라 faster-whisper. CTranslate2 백엔드라 같은 정확도에서
# CPU 추론이 4배 이상 빠르다. 데모는 GPU 없는 노트북에서 돌 가능성이 크다.
faster-whisper==1.2.1

# --- 콘텐츠 분석: LLM 화법 분류 ---
# 기획서가 "Claude API 또는 Gemini API"를 허용하므로 두 백엔드를 모두 넣는다.
# 어느 키도 없으면 키워드 규칙으로 폴백하므로, 키 없이도 파이프라인은 돈다.
# 백엔드 선택은 DUALGUARD_LLM_PROVIDER=auto|anthropic|gemini|openai|ollama (기본 auto).
# ollama는 pip 패키지가 필요 없다 — 표준 라이브러리 urllib.request로 로컬 서버
# (기본 localhost:11434)와 통신한다. openai/ollama 둘 다 auto 폴백 체인에는
# 안 들어가고 명시했을 때만 쓴다.
# .env에서 위 키들을 읽어들이는 데 쓴다(llm_classifier.py 모듈 최상단에서 로드).
# pydantic-settings의 전이 의존성으로 이미 깔려있었지만 명시적으로 고정한다.
python-dotenv==1.2.1
anthropic==0.121.0
# Gemini 백엔드. 구버전 google-generativeai가 아니라 신규 공식 SDK(google-genai)다.
# 둘은 API 모양이 완전히 다르니 섞어 쓰지 말 것.
google-genai==1.47.0
# OpenAI(GPT) 백엔드. Gemini 무료 티어 쿼터가 불안정해서(2026-09-02) 대체 옵션으로 추가.
openai==2.48.0

# --- 콘텐츠 분석: RAG 사례 검색 ---
# 임베딩 모델은 반드시 한국어 모델(jhgan/ko-sroberta-multitask)을 쓸 것.
# 다국어 모델은 무관한 한국어 문장을 동일 문장보다 가깝게 판정한다
# (근거: src/content_analysis/rag.py 상단 실측표).
chromadb==1.5.9
sentence-transformers==5.1.2

# --- 백엔드 API + 대시보드 ---
fastapi==0.128.8
uvicorn==0.39.0
python-multipart==0.0.20
